import type { DotGridConfig } from "@/types/home.types";

export const DOT_GRID_CONFIG: DotGridConfig = {
  dotSpacing: 24,
  dotRadius: 1,
  dotColor: "#d1d5db",
  mouseRadius: 500,
  repelStrength: 20,
  easeFactor: 0.1,
};

